[project]
name = "unicrawler-shared"
version = "0.1.0"
description = "Shared Python contracts and typed rows for UniCrawler services"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.12.0"]

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"
